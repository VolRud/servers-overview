import * as serversConstants from './servers';

const _ERROR = '_ERROR';
const _SUCCESS = '_SUCCESS';

const endpoints = {
	...serversConstants.serversEndpoints,
};

export {
	endpoints,
	serversConstants,
	_ERROR,
	_SUCCESS,
};